spring:
  application:
    name: jwt-security-gateway

  # ─────────────────────────────────────────────────────────
  #  12-Factor III: Store config in the environment
  #  All secrets are injected via env vars — never hardcoded
  # ─────────────────────────────────────────────────────────
  security:
    oauth2:
      resourceserver:
        jwt:
          # Keycloak / any OIDC provider — set OAUTH2_ISSUER_URI in your env
          issuer-uri: ${OAUTH2_ISSUER_URI:http://localhost:8180/realms/gateway}

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/gatewaydb}
    username: ${DB_USER:gateway_user}
    password: ${DB_PASSWORD:local_dev_password_only}
    hikari:
      # Keep pool small for Distroless/k3s — tune per workload
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      # Validate connection before use
      connection-test-query: SELECT 1

  jpa:
    hibernate:
      ddl-auto: validate          # Flyway owns the schema
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
        default_schema: public
    open-in-view: false           # 12-Factor-safe: no lazy loading leaks

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    url: ${DB_URL:jdbc:postgresql://localhost:5432/gatewaydb}
    user: ${FLYWAY_USER:postgres}
    password: ${FLYWAY_PASSWORD:${DB_PASSWORD:postgres}}

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

# ─────────────────────────────────────────────────────────
#  Actuator — k3s liveness and readiness probes
# ─────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics, prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

# ─────────────────────────────────────────────────────────
#  Server
# ─────────────────────────────────────────────────────────
server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful             # k3s-safe graceful pod termination
  compression:
    enabled: true

# ─────────────────────────────────────────────────────────
#  OpenAPI / Swagger UI
# ─────────────────────────────────────────────────────────
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true

# ─────────────────────────────────────────────────────────
#  Logging
# ─────────────────────────────────────────────────────────
logging:
  level:
    dev.gateway: DEBUG
    org.springframework.security: INFO
    org.flywaydb: INFO

# ─────────────────────────────────────────────────────────
#  Resilience4j — Circuit Breaker & Rate Limiter
# ─────────────────────────────────────────────────────────
resilience4j:
  # Circuit Breaker — wraps ProductController to handle downstream DB failures gracefully.
  # Rate limiting is handled at the infrastructure layer via a Redis Lua script
  # (GlobalRateLimiterService), NOT by Resilience4j. This gives us a distributed,
  # cluster-wide limit rather than a per-pod limit.
  circuitbreaker:
    instances:
      productCircuitBreaker:
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 10000ms
        permittedNumberOfCallsInHalfOpenState: 3
        minimumNumberOfCalls: 5
